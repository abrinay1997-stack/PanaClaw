---
/**
 * /ebot/ — la página del bot multicanal con panel.
 *
 * Es la primera página del sitio que vende algo que NO es una web, y eso cambia
 * dos cosas respecto a `/planes`:
 *
 * 1. **El precio no basta.** Con la cifra delante, la pregunta que frena no es
 *    «¿es caro?» sino «¿dónde está la trampa?». Por eso los dos gastos que no
 *    cobramos nosotros —la nube y la llave de IA— van publicados con el mismo
 *    tamaño que la implementación, y no en una nota al pie. Callarlos sería la
 *    letra chica que el resto del sitio dice no tener.
 * 2. **Hay que enseñar el panel por dentro.** Un plan de web se entiende sin
 *    verlo; una bandeja de mensajes, no. Los nombres de las trece pantallas son
 *    los reales, para que el día de la entrega no haya que buscar nada.
 *
 * REGLA 13 DEL PROYECTO: aquí NO va un bloque de cierre «¿Empezamos?» — ese vive
 * solo en /proceso. La salida de esta página es el CTA del nav, el del hero y el
 * del bloque de precio, y ni uno más.
 *
 * Todo el contenido sale de `src/data/ebot.ts`. Esta página no escribe una sola
 * cifra ni una sola característica a mano: lo que se cambie ahí cambia aquí, en
 * el desplegable de /contacto y en lo que responde el chat, en el mismo build.
 */
import BaseLayout from '../layouts/BaseLayout.astro';
import Reveal from '../components/Reveal.astro';
import SceneBg from '../components/SceneBg.astro';
import SectionNav from '../components/SectionNav.astro';

import {
  ebot,
  ebotCanales,
  ebotCapacidades,
  ebotPanel,
  ebotCostos,
  ebotIncluye,
  ebotNoIncluye,
  ebotPropiedad,
  ebotFaq,
  ebotContactHref,
} from '../data/ebot';
import { ebotVisual } from '../data/images';
import { routes } from '../data/links';
import { site, waLink } from '../data/site';

const origin = Astro.site ?? new URL('http://localhost:4321');
const ebotUrl = new URL(routes.ebot, origin).toString();

/** El precio va como número: schema.org no acepta "$70". */
const amount = Number(ebot.price.replace(/[^0-9.]/g, ''));

const serviceSchema = {
  '@context': 'https://schema.org',
  '@type': 'Service',
  name: `${ebot.name} — bot multicanal con panel`,
  serviceType: 'Implementación de asistente automatizado para atención al cliente',
  description: ebot.summary,
  areaServed: { '@type': 'Country', name: site.location },
  provider: {
    '@type': 'Organization',
    name: site.name,
    url: new URL(routes.home, origin).toString(),
  },
  offers: {
    '@type': 'Offer',
    name: `Implementación de ${ebot.name}`,
    description: ebotIncluye.join('. '),
    price: amount,
    priceCurrency: 'USD',
    url: ebotUrl,
    availability: 'https://schema.org/InStock',
  },
};

/*
 * Las preguntas de esta página también van como datos estructurados. No pisan
 * las de /ayuda: son otras preguntas, en otra URL, y ninguna se repite entre las
 * dos — si algún día se repitiera, se quita de una de las dos, porque el mismo
 * par pregunta/respuesta declarado en dos sitios es contenido duplicado.
 */
const faqSchema = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: ebotFaq.map((item) => ({
    '@type': 'Question',
    name: item.q,
    acceptedAnswer: { '@type': 'Answer', text: item.a },
  })),
};

const canalNames = ebotCanales.map((c) => c.name).join(', ');

/**
 * Cuántas pantallas trae el panel. Se cuenta, no se escribe: el titular anuncia
 * la cifra y una pantalla nueva en `ebot.ts` la dejaría desactualizada — que es
 * exactamente la clase de dato inventado que la regla 14 prohíbe.
 */
const pantallas = ebotPanel.reduce((n, g) => n + g.items.length, 0);
---

<BaseLayout
  title={`${ebot.name} — bot multicanal`}
  description={`${ebot.name}: contesta por ${canalNames} las 24 horas y guarda cada conversación y cada cliente en un panel tuyo. Implementación ${ebot.price}, pago único, sin mensualidad nuestra.`}
  activePath={routes.ebot}
  jsonLd={[serviceSchema, faqSchema]}
>
  <!-- HERO -->
  <section class="ebot-hero">
    <SceneBg visual={ebotVisual} side="left" />
    <div class="container">
      <div class="ebot-hero-copy">
        <Reveal eager>
          <span class="eyebrow">{ebot.name} · Bot multicanal con panel</span>
          <h1>Todos tus mensajes, <span class="muted">en un solo lugar.</span></h1>
        </Reveal>
        <Reveal delay={0.12} eager>
          <p class="ebot-lead">{ebot.summary}</p>
        </Reveal>
        <Reveal delay={0.2} eager>
          <div class="ebot-hero-price">
            <span class="ebot-hero-amount">{ebot.price}</span>
            <span class="ebot-hero-kicker">{ebot.priceKicker}</span>
          </div>
          <div class="ebot-hero-actions">
            <a class="btn btn-primary" href={ebotContactHref}>
              <span class="dot"></span> Pedir {ebot.name}
            </a>
            <a
              class="btn btn-ghost"
              href={waLink(`Hola ${site.name}, quiero ${ebot.name} para mi negocio. Mi negocio es: `)}
              target="_blank"
              rel="noopener"
            >
              Preguntar por WhatsApp
            </a>
          </div>
        </Reveal>
      </div>
    </div>
  </section>

  <!-- CANALES: lo primero que hay que creer es que entra por donde te escriben -->
  <section class="ebot-canales" id="canales">
    <div class="container">
      <Reveal>
        <span class="eyebrow">Los cuatro canales</span>
        <h2 class="section-title">Contesta por donde <span class="muted">ya te escriben.</span></h2>
      </Reveal>
      <Reveal>
        <p class="ebot-intro">
          No hay que enseñarle a nadie a usar una aplicación nueva. Tu cliente escribe donde
          siempre escribió y le contestan igual de rápido a las tres de la tarde que a las once de
          la noche. Tú lo ves todo desde una sola pantalla.
        </p>
      </Reveal>

      <!--
        El índice va arriba, antes de las tarjetas, y no al final de la sección:
        esta es con diferencia la página más larga del sitio (medido abajo, en
        docs/ESTADO.md) y un índice al que se llega scrolleando 2 400 px en el
        teléfono no le ahorra el scroll a nadie. Las dos reglas de <SectionNav>
        se respetan: cada destino declara su `id` y su `scroll-margin-top`.
      -->
      <div class="ebot-index">
        <span class="ebot-index-label">Salta a lo que viniste a ver</span>
        <SectionNav
          label={`Secciones de ${ebot.name}`}
          items={[
            { href: '#hace', label: 'Qué hace' },
            { href: '#panel', label: 'El panel por dentro' },
            { href: '#precio', label: 'Qué cuesta de verdad' },
            { href: '#preguntas', label: 'Preguntas' },
          ]}
        />
      </div>

      <div class="ebot-canal-grid">
        {ebotCanales.map((canal) => (
          <Reveal>
            <div class="ebot-canal">
              <h3>{canal.name}</h3>
              <p>{canal.desc}</p>
              {canal.nota && <p class="ebot-canal-nota">{canal.nota}</p>}
            </div>
          </Reveal>
        ))}
      </div>
    </div>
  </section>

  <!-- QUÉ HACE -->
  <section class="ebot-hace" id="hace">
    <div class="container">
      <Reveal>
        <span class="eyebrow">Qué hace</span>
        <h2 class="section-title">Las mismas cinco preguntas, <span class="muted">contestadas sin ti.</span></h2>
      </Reveal>

      <div class="ebot-hace-grid">
        {ebotCapacidades.map((cap, i) => (
          <Reveal>
            <div class="ebot-cap">
              <span class="ebot-cap-idx">{String(i + 1).padStart(2, '0')}</span>
              <h3>{cap.title}</h3>
              <p>{cap.desc}</p>
            </div>
          </Reveal>
        ))}
      </div>
    </div>
  </section>

  <!-- EL PANEL -->
  <section class="ebot-panel" id="panel">
    <div class="container">
      <Reveal>
        <span class="eyebrow">El panel por dentro</span>
        <h2 class="section-title">{pantallas} pantallas. <span class="muted">Estas son, con su nombre real.</span></h2>
      </Reveal>
      <Reveal>
        <p class="ebot-intro">
          Entras con tu contraseña desde el teléfono o la computadora. Lo que ves aquí abajo es
          exactamente lo que vas a encontrar adentro, y en el mismo orden — nada de nombres
          bonitos para la página de ventas.
        </p>
      </Reveal>

      <div class="ebot-panel-grid">
        {ebotPanel.map((group) => (
          <Reveal>
            <div class="ebot-panel-group">
              <h3>{group.title}</h3>
              <ul>
                {group.items.map((item) => (
                  <li>
                    <span class="ebot-panel-name">{item.name}</span>
                    <span class="ebot-panel-desc">{item.desc}</span>
                  </li>
                ))}
              </ul>
            </div>
          </Reveal>
        ))}
      </div>
    </div>
  </section>

  <!-- PRECIO: lo nuestro y lo que no es nuestro, al mismo tamaño -->
  <section class="ebot-precio" id="precio">
    <div class="container">
      <Reveal>
        <span class="eyebrow">Qué cuesta de verdad</span>
        <h2 class="section-title">{ebot.price} una vez. <span class="muted">Y dos cuentas que no nos pagas a nosotros.</span></h2>
      </Reveal>

      <Reveal>
        <div class="ebot-precio-card">
          <div class="ebot-precio-copy">
            <h3>Lo que nos pagas a nosotros</h3>
            <p class="ebot-precio-desc">
              Una sola vez, por montarlo y dejarlo funcionando en tu cuenta. No hay cuota de
              plataforma, ni límite de mensajes, ni un precio que suba el año que viene.
            </p>
            <ul class="ebot-check">
              {ebotIncluye.map((item) => <li>{item}</li>)}
            </ul>
          </div>
          <div class="ebot-precio-tag">
            <span class="ebot-precio-v">{ebot.price}</span>
            <span class="ebot-precio-k">{ebot.priceKicker}</span>
            <a class="btn btn-primary" href={ebotContactHref}>
              <span class="dot"></span> Pedir {ebot.name}
            </a>
          </div>
        </div>
      </Reveal>

      <div class="ebot-costos">
        <Reveal>
          <h3 class="ebot-sub">Lo que pagas por tu cuenta, cada mes</h3>
          <p class="ebot-intro">
            Estas dos no pasan por nosotros: las contratas tú, a tu nombre, y son las que hacen
            que el bot sea tuyo de verdad. Son aproximadas y para un negocio normal; el panel te
            enseña el gasto al día para que nunca sea una sorpresa.
          </p>
        </Reveal>
        <div class="ebot-costo-grid">
          {ebotCostos.map((costo) => (
            <Reveal>
              <div class="ebot-costo">
                <span class="ebot-costo-k">{costo.concepto}</span>
                <span class="ebot-costo-v">{costo.price}</span>
                <span class="ebot-costo-a">{costo.aQuien}</span>
                <p>{costo.desc}</p>
              </div>
            </Reveal>
          ))}
        </div>
      </div>

      <Reveal>
        <div class="ebot-no">
          <h3 class="ebot-sub">Lo que no está incluido</h3>
          <ul class="ebot-cross">
            {ebotNoIncluye.map((item) => <li>{item}</li>)}
          </ul>
        </div>
      </Reveal>
    </div>
  </section>

  <!-- PROPIEDAD -->
  <section class="ebot-propiedad">
    <div class="container">
      <div class="ebot-propiedad-grid">
        <Reveal>
          <h2 class="ebot-propiedad-title">Es tuyo. <span class="muted">Igual que el código de tu sitio.</span></h2>
        </Reveal>
        <div class="ebot-propiedad-facts">
          {ebotPropiedad.map((hecho, i) => (
            <Reveal delay={0.1 + i * 0.08}>
              <div class="ebot-propiedad-fact">
                <span class="ebot-propiedad-k">{hecho.k}</span>
                <p>{hecho.desc}</p>
              </div>
            </Reveal>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- PREGUNTAS -->
  <section class="ebot-faq" id="preguntas">
    <div class="container">
      <Reveal>
        <span class="eyebrow">Preguntas</span>
        <h2 class="section-title">Lo que se pregunta <span class="muted">antes de decir que sí.</span></h2>
      </Reveal>

      <div class="ebot-faq-items">
        {ebotFaq.map((item) => (
          <Reveal>
            <details class="ebot-faq-item">
              <summary>
                <span>{item.q}</span>
                <span class="ebot-mark" aria-hidden="true"></span>
              </summary>
              <p>{item.a}</p>
            </details>
          </Reveal>
        ))}
      </div>

      <Reveal>
        <p class="ebot-faq-foot">
          ¿Otra duda? Está el <a href={routes.ayuda}>centro de ayuda</a>, y si tampoco está ahí,
          contesta la misma persona que montaría tu bot.
        </p>
      </Reveal>
    </div>
  </section>
</BaseLayout>

<style>
  /* ---------- HERO ---------- */
  /* Sin `.hero` global: esa clase trae el halo naranja del hero de la home, y
     aquí el fondo lo pone <SceneBg>. Dos fuentes de luz peleando dejaban el
     titular sin contraste justo en la mitad izquierda. */
  .ebot-hero {
    position: relative;
    display: flex;
    align-items: center;
    min-height: 88vh;
    padding: 168px 0 110px;
    overflow: hidden;
  }
  .ebot-hero .container {
    position: relative;
    z-index: 1;
  }
  .ebot-hero-copy {
    max-width: 54ch;
  }
  /* Medido a 1440×900: con 88px y 15ch el titular ocupaba cinco líneas y
     empujaba el precio y los dos botones por debajo del pliegue. En una página
     que vende un producto que nadie conoce, la cifra tiene que verse sin
     scrollear — es la que decide si se sigue leyendo. */
  .ebot-hero h1 {
    font-size: clamp(38px, 5vw, 74px);
    font-weight: 600;
    line-height: 0.98;
    letter-spacing: -0.025em;
    text-transform: uppercase;
    max-width: 17ch;
    text-shadow: 0 4px 40px rgba(16, 1, 1, 0.85);
  }
  .ebot-hero h1 .muted {
    color: var(--studio-gray);
  }
  .ebot-lead {
    margin-top: 26px;
    max-width: 56ch;
    font-size: 18px;
    font-weight: 300;
    color: var(--studio-gray);
    line-height: 1.65;
    text-shadow: 0 2px 24px rgba(16, 1, 1, 0.9);
  }
  /* El precio va en el hero y no solo en su bloque: es la cifra que decide si
     alguien sigue leyendo, y en un producto desconocido decidirla tarde es
     perder a quien se imaginó una mensualidad. */
  .ebot-hero-price {
    display: flex;
    align-items: baseline;
    flex-wrap: wrap;
    gap: 8px 18px;
    margin-top: 34px;
  }
  .ebot-hero-amount {
    font-size: clamp(44px, 5.6vw, 68px);
    font-weight: 700;
    letter-spacing: -0.03em;
    line-height: 1;
  }
  .ebot-hero-kicker {
    font-size: 13px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--flash-orange);
  }
  .ebot-hero-actions {
    display: flex;
    gap: 14px;
    flex-wrap: wrap;
    margin-top: 34px;
  }

  /* ---------- CANALES ---------- */
  .ebot-canales {
    padding-bottom: 100px;
  }
  .ebot-intro {
    max-width: 62ch;
    margin-bottom: 44px;
    font-size: 17px;
    font-weight: 300;
    color: var(--studio-gray);
    line-height: 1.7;
  }
  .ebot-canal-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 18px;
  }
  .ebot-canal {
    height: 100%;
    padding: 30px 26px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 20px;
    background: rgba(255, 255, 255, 0.02);
    transition: border-color 0.25s, background 0.25s;
  }
  .ebot-canal:hover {
    border-color: var(--flash-orange);
    background: rgba(255, 81, 0, 0.06);
  }
  .ebot-canal h3 {
    font-size: 21px;
    font-weight: 600;
    letter-spacing: -0.01em;
    margin-bottom: 12px;
  }
  .ebot-canal p {
    font-size: 15px;
    font-weight: 300;
    color: var(--studio-gray);
    line-height: 1.6;
  }
  /* El trámite que no depende de nosotros se marca, no se esconde: quien lee
     rápido tiene que verlo sin leerlo entero. */
  .ebot-canal-nota {
    margin-top: 14px;
    padding-top: 14px;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    font-size: 13.5px;
  }

  .ebot-index {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 14px 20px;
    margin-bottom: 44px;
    padding-bottom: 34px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  }
  .ebot-index-label {
    font-size: 13px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--studio-gray);
  }

  /* ---------- QUÉ HACE ---------- */
  /* Los cuatro destinos del índice necesitan margen: el nav flotante mide 110px
     y sin esto el salto deja el titular debajo de la barra. */
  .ebot-hace,
  .ebot-panel,
  .ebot-precio,
  .ebot-faq,
  .ebot-canales {
    scroll-margin-top: 110px;
  }
  .ebot-hace {
    padding-top: 20px;
  }
  .ebot-hace-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 22px;
  }
  .ebot-cap {
    height: 100%;
    padding: 32px 28px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 20px;
    background: rgba(255, 255, 255, 0.02);
    transition: border-color 0.25s, background 0.25s;
  }
  .ebot-cap:hover {
    border-color: var(--flash-orange);
    background: rgba(255, 81, 0, 0.05);
  }
  .ebot-cap-idx {
    display: block;
    font-size: 13px;
    letter-spacing: 0.16em;
    color: var(--flash-orange);
  }
  .ebot-cap h3 {
    margin: 14px 0 12px;
    font-size: 22px;
    font-weight: 600;
    letter-spacing: -0.01em;
    line-height: 1.15;
  }
  .ebot-cap p {
    font-size: 15px;
    font-weight: 300;
    color: var(--studio-gray);
    line-height: 1.65;
  }

  /* ---------- PANEL ---------- */
  .ebot-panel-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 22px;
  }
  .ebot-panel-group {
    height: 100%;
    padding: 30px 28px;
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: 20px;
    background: rgba(255, 255, 255, 0.03);
  }
  .ebot-panel-group h3 {
    font-size: 13px;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--flash-orange);
    margin-bottom: 22px;
  }
  .ebot-panel-group ul {
    list-style: none;
    display: grid;
    gap: 16px;
  }
  .ebot-panel-group li {
    padding-bottom: 16px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.07);
  }
  .ebot-panel-group li:last-child {
    padding-bottom: 0;
    border-bottom: none;
  }
  .ebot-panel-name {
    display: block;
    font-size: 16px;
    font-weight: 500;
  }
  .ebot-panel-desc {
    display: block;
    margin-top: 4px;
    font-size: 14px;
    font-weight: 300;
    color: var(--studio-gray);
    line-height: 1.55;
  }

  /* ---------- PRECIO ---------- */
  .ebot-precio-card {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 44px;
    align-items: center;
    padding: 44px 46px;
    border-radius: 24px;
    background: linear-gradient(120deg, rgba(255, 81, 0, 0.16), rgba(238, 0, 0, 0.06));
    border: 1px solid rgba(255, 81, 0, 0.4);
  }
  .ebot-precio-copy h3 {
    font-size: clamp(24px, 3vw, 34px);
    font-weight: 600;
    letter-spacing: -0.02em;
    line-height: 1.1;
    margin-bottom: 14px;
  }
  .ebot-precio-desc {
    max-width: 58ch;
    font-size: 16px;
    font-weight: 300;
    color: var(--soft-white);
    line-height: 1.65;
    margin-bottom: 22px;
  }
  .ebot-precio-tag {
    text-align: right;
  }
  .ebot-precio-v {
    display: block;
    font-size: clamp(48px, 6vw, 76px);
    font-weight: 700;
    letter-spacing: -0.03em;
    line-height: 1;
  }
  .ebot-precio-k {
    display: block;
    margin: 10px 0 22px;
    font-size: 12px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--studio-gray);
  }

  /* Listas de sí y de no. El símbolo va en `::before` y no en el texto para que
     el lector de pantalla lea la frase y no un carácter suelto. */
  .ebot-check,
  .ebot-cross {
    list-style: none;
    display: grid;
    gap: 10px;
  }
  .ebot-check li,
  .ebot-cross li {
    position: relative;
    padding-left: 26px;
    font-size: 15.5px;
    font-weight: 300;
    line-height: 1.55;
  }
  .ebot-check li {
    color: var(--soft-white);
  }
  .ebot-cross li {
    color: var(--studio-gray);
  }
  .ebot-check li::before,
  .ebot-cross li::before {
    position: absolute;
    left: 0;
    top: 0;
    font-weight: 500;
  }
  .ebot-check li::before {
    content: '✓';
    color: var(--flash-orange);
  }
  .ebot-cross li::before {
    content: '×';
    color: var(--studio-gray);
  }

  .ebot-costos {
    margin-top: 64px;
  }
  .ebot-sub {
    font-size: clamp(22px, 2.6vw, 30px);
    font-weight: 600;
    letter-spacing: -0.02em;
    margin-bottom: 16px;
  }
  .ebot-costo-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 18px;
  }
  .ebot-costo {
    height: 100%;
    padding: 30px 28px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 20px;
    background: rgba(255, 255, 255, 0.02);
  }
  .ebot-costo-k {
    display: block;
    font-size: 13px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--studio-gray);
  }
  .ebot-costo-v {
    display: block;
    margin-top: 10px;
    font-size: clamp(30px, 3.4vw, 42px);
    font-weight: 700;
    letter-spacing: -0.03em;
    line-height: 1;
  }
  .ebot-costo-a {
    display: block;
    margin-top: 10px;
    font-size: 13px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--flash-orange);
  }
  .ebot-costo p {
    margin-top: 16px;
    font-size: 15px;
    font-weight: 300;
    color: var(--studio-gray);
    line-height: 1.65;
  }

  .ebot-no {
    margin-top: 54px;
    padding: 34px 36px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 22px;
    background: rgba(255, 255, 255, 0.02);
  }

  /* ---------- PROPIEDAD ---------- */
  .ebot-propiedad {
    border-top: 1px solid rgba(255, 255, 255, 0.06);
    padding: 100px 0;
  }
  .ebot-propiedad-grid {
    display: grid;
    grid-template-columns: 0.9fr 1.1fr;
    gap: 60px;
    align-items: start;
  }
  .ebot-propiedad-title {
    font-size: clamp(32px, 4.2vw, 56px);
    font-weight: 600;
    line-height: 1.02;
    letter-spacing: -0.02em;
    text-transform: uppercase;
  }
  .ebot-propiedad-title .muted {
    color: var(--studio-gray);
  }
  .ebot-propiedad-facts {
    display: grid;
    gap: 28px;
  }
  .ebot-propiedad-k {
    display: block;
    font-size: 13px;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--flash-orange);
    margin-bottom: 10px;
  }
  .ebot-propiedad-fact p {
    max-width: 56ch;
    font-size: 16px;
    font-weight: 300;
    color: var(--studio-gray);
    line-height: 1.65;
  }

  /* ---------- PREGUNTAS ---------- */
  /* Mismo acordeón que /ayuda: es el patrón que el sitio ya tiene y copiar su
     comportamiento evita que dos preguntas frecuentes se abran distinto. */
  .ebot-faq-items {
    display: grid;
    gap: 2px;
    max-width: 90ch;
  }
  .ebot-faq-item {
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  }
  .ebot-faq-item summary {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 24px;
    padding: 20px 0;
    font-size: 17px;
    font-weight: 500;
    line-height: 1.4;
    cursor: pointer;
    list-style: none;
  }
  .ebot-faq-item summary::-webkit-details-marker {
    display: none;
  }
  .ebot-faq-item summary:hover {
    color: var(--flash-orange);
  }
  .ebot-mark {
    position: relative;
    flex-shrink: 0;
    width: 16px;
    height: 16px;
  }
  .ebot-mark::before,
  .ebot-mark::after {
    content: '';
    position: absolute;
    inset: 0;
    margin: auto;
    background: var(--flash-orange);
    transition: transform 0.25s ease;
  }
  .ebot-mark::before {
    width: 16px;
    height: 1.5px;
  }
  .ebot-mark::after {
    width: 1.5px;
    height: 16px;
  }
  .ebot-faq-item[open] .ebot-mark::after {
    transform: rotate(90deg);
  }
  .ebot-faq-item p {
    padding: 0 0 24px;
    max-width: 68ch;
    font-size: 16px;
    font-weight: 300;
    color: var(--studio-gray);
    line-height: 1.75;
  }
  .ebot-faq-foot {
    margin-top: 34px;
    font-size: 15px;
    font-weight: 300;
    color: var(--studio-gray);
  }
  .ebot-faq-foot a {
    color: var(--flash-orange);
/* Subrayado obligado: el naranja sobre el gris del cuerpo da 1,68:1 y el
       mínimo para distinguir un enlace solo por color es 3:1. Sin esto, en
       medio de un párrafo no se ve que sea un enlace (Lighthouse
       "link-in-text-block", 22 ago 2026). */
    text-decoration: underline;
    text-underline-offset: 0.16em;
    text-decoration-thickness: 1px;
  }

  /* ---------- RESPONSIVE ---------- */
  @media (max-width: 1024px) {
    .ebot-canal-grid {
      grid-template-columns: repeat(2, 1fr);
    }
    .ebot-hace-grid,
    .ebot-panel-grid {
      grid-template-columns: 1fr;
    }
  }
  @media (max-width: 900px) {
    .ebot-hero {
      min-height: 0;
      padding: 150px 0 90px;
    }
    .ebot-hero-copy {
      max-width: none;
    }
    .ebot-precio-card {
      grid-template-columns: 1fr;
      gap: 26px;
      padding: 32px 26px;
    }
    .ebot-precio-tag {
      text-align: left;
    }
    .ebot-costo-grid,
    .ebot-propiedad-grid {
      grid-template-columns: 1fr;
      gap: 26px;
    }
    .ebot-no {
      padding: 28px 24px;
    }
    .ebot-propiedad {
      padding: 80px 0;
    }
  }
  /*
    En el teléfono todo se apila y la página se convierte en la más larga del
    sitio. No se recorta contenido para arreglarlo —lo que hay que leer para
    comprar un bot es lo que hay—, se recorta el aire: relleno de tarjeta y hueco
    entre ellas. Son ~1 200 px menos de scroll sin quitar una sola frase.
  */
  @media (max-width: 640px) {
    .ebot-canal-grid {
      grid-template-columns: 1fr;
    }
    .ebot-canal-grid,
    .ebot-hace-grid,
    .ebot-panel-grid,
    .ebot-costo-grid {
      gap: 12px;
    }
    .ebot-canal,
    .ebot-cap,
    .ebot-panel-group,
    .ebot-costo {
      padding: 24px 22px;
    }
    .ebot-intro {
      margin-bottom: 32px;
    }
    .ebot-index {
      margin-bottom: 32px;
      padding-bottom: 26px;
    }
    .ebot-costos {
      margin-top: 44px;
    }
    .ebot-no {
      margin-top: 38px;
    }
  }
</style>

<style is:global>
  /* <Reveal> mete un <div> entre la grid y la tarjeta, así que la tarjeta deja
     de ser el item de grid y no se estira a la altura de la fila. Mismo apaño
     que `global.css` aplica a las grids del resto del sitio, aquí con los
     nombres de esta página. Va en `is:global` porque el nodo intermedio lo
     genera el componente y no lleva el `data-astro-cid` de esta página. */
  .ebot-canal-grid > .reveal,
  .ebot-hace-grid > .reveal,
  .ebot-panel-grid > .reveal,
  .ebot-costo-grid > .reveal {
    display: flex;
    flex-direction: column;
  }
  .ebot-canal-grid > .reveal > *,
  .ebot-hace-grid > .reveal > *,
  .ebot-panel-grid > .reveal > *,
  .ebot-costo-grid > .reveal > * {
    flex: 1 1 auto;
  }
</style>
